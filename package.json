{
  "name": "pasarela-pagos",
  "version": "1.0.0",
  "description": "Pasarela de pagos con PayPal y NOWPayments",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "reconcile": "node reconcile.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.3.1",
    "pg": "^8.11.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
